<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1>Welcome to the Mini Message Board</h1>
    <p>
      This is a simple message board made with Node.js, postgreSQL and Express.
    </p>
    <p>Here are the messages:</p>
    <ul>
      <% messages.forEach((message, index) => { %>
      <li>
        <strong><%= message.username %>:</strong> <%= message.text %> <br />
        <a href="/message/<%= message.id %>">Open</a> <strong>|</strong>
        <a href="/message/delete/<%= message.id %>">delete</a>
        <!-- <a href="/message/edit/<%= message.id %>">EDIT</a> -->
      </li>
      <% }) %>
    </ul>
    <a href="/message/new">
      <span> chat </span>
    </a>
  </body>
</html>
